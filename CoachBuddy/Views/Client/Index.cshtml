@model IEnumerable<CoachBuddy.Application.Client.ClientDto>

@{
    ViewData["Title"] = "Clients";
}

<h1>Clients</h1>

<p>
    <a asp-action="Create" class="btn btn-secondary">Add new client</a>
</p>

<table class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Name</th>
            <th>Phone Number</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.PhoneNumber</td>
                <td>@item.Description</td>
                <td>
                    <a class="btn btn-primary btn-sm" asp-controller="Client" asp-action="Details" asp-route-encodedName="@item.EncodedName">Details</a>
                    @if (item.IsEditable)
                    {
                        <a class="btn btn-outline-secondary btn-sm" asp-controller="Client" asp-action="Edit" asp-route-encodedName="@item.EncodedName">Edit</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
